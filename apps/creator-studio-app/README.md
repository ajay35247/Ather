# @ather/creator-studio-app

Creator Studio frontend. Distinct from `services/creator-studio` (backend service). Scaffold only — populate `src/` with `dashboard/`, `earnings/`, `analytics/`, `uploads/`, `ai-tools/`, `audience/`, `scheduling/`, `monetization/`.
