<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for API >= 31.
    Used by android:dataExtractionRules in AndroidManifest.xml.

    Two independent rule sets are required: one for cloud backup (Google
    Drive Auto Backup) and one for device-to-device transfer. The same
    sensitive paths are excluded from both.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="app_webview/" />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="no_backup/" />
        <exclude domain="sharedpref" path="CapacitorStorage.xml" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="app_webview/" />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="no_backup/" />
        <exclude domain="sharedpref" path="CapacitorStorage.xml" />
    </device-transfer>
</data-extraction-rules>
