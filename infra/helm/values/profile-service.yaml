# Auto-aligned with services/profile-service/src/index.ts (port 4002).
# Deploy with: helm upgrade --install profile-service infra/helm/_service \
#                -f infra/helm/values/profile-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: identity
  port: 80
  targetPort: 4002

image:
  repository: ghcr.io/ajay35247/ather-profile-service
