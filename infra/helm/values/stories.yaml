# Auto-aligned with services/stories/src/index.ts (port 4021).
# Deploy with: helm upgrade --install stories infra/helm/_service \
#                -f infra/helm/values/stories.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: content
  port: 80
  targetPort: 4021

image:
  repository: ghcr.io/ajay35247/ather-stories
