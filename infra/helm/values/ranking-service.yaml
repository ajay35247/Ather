# Auto-aligned with services/ranking-service/src/index.ts (port 4025).
# Deploy with: helm upgrade --install ranking-service infra/helm/_service \
#                -f infra/helm/values/ranking-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: ml
  port: 80
  targetPort: 4025

image:
  repository: ghcr.io/ajay35247/ather-ranking-service
