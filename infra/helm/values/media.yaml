# Auto-aligned with services/media/src/index.ts (port 4006).
# Deploy with: helm upgrade --install media infra/helm/_service \
#                -f infra/helm/values/media.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: content
  port: 80
  targetPort: 4006

image:
  repository: ghcr.io/ajay35247/ather-media
