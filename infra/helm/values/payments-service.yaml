# Auto-aligned with services/payments-service/src/index.ts (port 4031).
# Deploy with: helm upgrade --install payments-service infra/helm/_service \
#                -f infra/helm/values/payments-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: economy
  port: 80
  targetPort: 4031

image:
  repository: ghcr.io/ajay35247/ather-payments-service

internalSecret:
  enabled: true
