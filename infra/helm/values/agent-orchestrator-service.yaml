# Auto-aligned with services/agent-orchestrator-service/src/index.ts (port 4055).
# Deploy with: helm upgrade --install agent-orchestrator-service infra/helm/_service \
#                -f infra/helm/values/agent-orchestrator-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: ml
  port: 80
  targetPort: 4055

image:
  repository: ghcr.io/ajay35247/ather-agent-orchestrator-service
