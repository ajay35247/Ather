# Auto-aligned with services/communities-service/src/index.ts (port 4023).
# Deploy with: helm upgrade --install communities-service infra/helm/_service \
#                -f infra/helm/values/communities-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: social
  port: 80
  targetPort: 4023

image:
  repository: ghcr.io/ajay35247/ather-communities-service
