# Auto-aligned with services/tips/src/index.ts (port 4033).
# Deploy with: helm upgrade --install tips infra/helm/_service \
#                -f infra/helm/values/tips.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: economy
  port: 80
  targetPort: 4033

image:
  repository: ghcr.io/ajay35247/ather-tips
