# Auto-aligned with services/recommendations/src/index.ts (port 4026).
# Deploy with: helm upgrade --install recommendations infra/helm/_service \
#                -f infra/helm/values/recommendations.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: ml
  port: 80
  targetPort: 4026

image:
  repository: ghcr.io/ajay35247/ather-recommendations
