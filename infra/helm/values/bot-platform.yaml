# Auto-aligned with services/bot-platform/src/index.ts (port 4052).
# Deploy with: helm upgrade --install bot-platform infra/helm/_service \
#                -f infra/helm/values/bot-platform.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: platform
  port: 80
  targetPort: 4052

image:
  repository: ghcr.io/ajay35247/ather-bot-platform

internalSecret:
  enabled: true
