# Auto-aligned with services/bot-platform-service/src/index.ts (port 4052).
# Deploy with: helm upgrade --install bot-platform-service infra/helm/_service \
#                -f infra/helm/values/bot-platform-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: platform
  port: 80
  targetPort: 4052

image:
  repository: ghcr.io/ajay35247/ather-bot-platform-service

internalSecret:
  enabled: true
