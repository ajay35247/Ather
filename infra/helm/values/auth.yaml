# Auto-aligned with services/auth/src/index.ts (port 4001).
# Deploy with: helm upgrade --install auth infra/helm/_service \
#                -f infra/helm/values/auth.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: identity
  port: 80
  targetPort: 4001

image:
  repository: ghcr.io/ajay35247/ather-auth
