# Auto-aligned with services/mini-app-runtime-service/src/index.ts (port 4050).
# Deploy with: helm upgrade --install mini-app-runtime-service infra/helm/_service \
#                -f infra/helm/values/mini-app-runtime-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: platform
  port: 80
  targetPort: 4050

image:
  repository: ghcr.io/ajay35247/ather-mini-app-runtime-service

internalSecret:
  enabled: true
