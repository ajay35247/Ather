# Auto-aligned with services/groups/src/index.ts (port 4024).
# Deploy with: helm upgrade --install groups infra/helm/_service \
#                -f infra/helm/values/groups.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: social
  port: 80
  targetPort: 4024

image:
  repository: ghcr.io/ajay35247/ather-groups
