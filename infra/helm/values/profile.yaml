# Auto-aligned with services/profile/src/index.ts (port 4002).
# Deploy with: helm upgrade --install profile infra/helm/_service \
#                -f infra/helm/values/profile.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: identity
  port: 80
  targetPort: 4002

image:
  repository: ghcr.io/ajay35247/ather-profile
