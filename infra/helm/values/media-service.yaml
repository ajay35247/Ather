# Auto-aligned with services/media-service/src/index.ts (port 4006).
# Deploy with: helm upgrade --install media-service infra/helm/_service \
#                -f infra/helm/values/media-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: content
  port: 80
  targetPort: 4006

image:
  repository: ghcr.io/ajay35247/ather-media-service
