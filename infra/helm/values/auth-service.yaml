# Auto-aligned with services/auth-service/src/index.ts (port 4001).
# Deploy with: helm upgrade --install auth-service infra/helm/_service \
#                -f infra/helm/values/auth-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: identity
  port: 80
  targetPort: 4001

image:
  repository: ghcr.io/ajay35247/ather-auth-service
