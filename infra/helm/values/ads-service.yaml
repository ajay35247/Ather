# Auto-aligned with services/ads-service/src/index.ts (port 4034).
# Deploy with: helm upgrade --install ads-service infra/helm/_service \
#                -f infra/helm/values/ads-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: economy
  port: 80
  targetPort: 4034

image:
  repository: ghcr.io/ajay35247/ather-ads-service
