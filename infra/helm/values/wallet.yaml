# Auto-aligned with services/wallet/src/index.ts (port 4030).
# Deploy with: helm upgrade --install wallet infra/helm/_service \
#                -f infra/helm/values/wallet.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: economy
  port: 80
  targetPort: 4030

image:
  repository: ghcr.io/ajay35247/ather-wallet

internalSecret:
  enabled: true
