# Auto-aligned with services/content-events-service/src/index.ts (port 4027).
# Deploy with: helm upgrade --install content-events-service infra/helm/_service \
#                -f infra/helm/values/content-events-service.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: platform
  port: 80
  targetPort: 4027

image:
  repository: ghcr.io/ajay35247/ather-content-events-service

internalSecret:
  enabled: true
