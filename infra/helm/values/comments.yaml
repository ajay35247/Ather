# Auto-aligned with services/comments/src/index.ts (port 4022).
# Deploy with: helm upgrade --install comments infra/helm/_service \
#                -f infra/helm/values/comments.yaml \
#                --set image.tag=$GITHUB_SHA

service:
  plane: content
  port: 80
  targetPort: 4022

image:
  repository: ghcr.io/ajay35247/ather-comments
