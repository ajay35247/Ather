# kafka/

Kafka topic definitions, producers, consumers, schemas, and retry/DLQ configs shared across services.

- `topics/` — topic specs (name, partitions, retention)
- `producers/` `consumers/` — reference implementations
- `schemas/` — Avro / JSON Schema / Protobuf
- `retries/` — retry & DLQ configurations

Scaffold.
