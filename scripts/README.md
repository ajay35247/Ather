# scripts/

Operational scripts grouped by concern.

- `setup/` — local dev bootstrap
- `migrations/` — schema migration runners
- `deployment/` — deploy helpers
- `backup/` — DB / object-storage backups
- `testing/` — test orchestration helpers
- `seeding/` — seed-data loaders
- `cleanup/` — periodic cleanup jobs

Scaffold.
