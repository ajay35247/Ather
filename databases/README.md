# databases/

Database schemas, seeds, and migration sources by engine.

> **Note:** Production Postgres migrations currently live in
> [`infra/postgres/migrations/`](../infra/postgres/migrations/).
> This directory is a scaffold for engine-specific assets (Mongo collections,
> Redis Lua scripts, ClickHouse DDL, Elasticsearch index templates,
> vector-DB schemas).

- `postgres/` `mongodb/` `redis/` `elasticsearch/` `clickhouse/` `vector-db/`
